import { RepeatInfo } from '../../types';

/**
 * 반복 정보를 텍스트로 변환합니다.
 *
 * @param repeat - 반복 정보
 * @returns 반복 정보 텍스트 (예: "반복: 1일마다", "반복: 1주마다 (종료: 2025-12-31)")
 */
export function getRepeatText(repeat: RepeatInfo): string {
  // TODO: 구현 필요
  return '';
}
