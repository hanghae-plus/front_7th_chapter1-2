```typescript
// filepath: src/__tests__/unit/example-feature.spec.ts

import { describe, it, expect } from 'vitest';
import { exampleFeature } from '../../utils/example-feature';

describe('Example Feature', () => {
  describe('Normal Cases', () => {
    it('should return valid result with positive number', () => {
      // Arrange
      const input = 5;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(true);
      expect(result.value).toBe(10);
    });

    it('should return valid result with zero', () => {
      // Arrange
      const input = 0;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(true);
      expect(result.value).toBe(0);
    });

    it('should return valid result with negative number', () => {
      // Arrange
      const input = -5;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(true);
      expect(result.value).toBe(-10);
    });

    it('should return valid result with decimal number', () => {
      // Arrange
      const input = 3.5;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(true);
      expect(result.value).toBe(7);
    });

    it('should return valid result with large number', () => {
      // Arrange
      const input = 1000000;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(true);
      expect(result.value).toBe(2000000);
    });
  });

  describe('Error Cases', () => {
    it('should handle null input', () => {
      // Arrange
      const input = null as any;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle undefined input', () => {
      // Arrange
      const input = undefined as any;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle string input', () => {
      // Arrange
      const input = 'invalid' as any;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle NaN input', () => {
      // Arrange
      const input = NaN;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle object input', () => {
      // Arrange
      const input = { value: 5 } as any;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle array input', () => {
      // Arrange
      const input = [1, 2, 3] as any;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });
  });

  describe('Edge Cases', () => {
    it('should handle Infinity', () => {
      // Arrange
      const input = Infinity;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle negative Infinity', () => {
      // Arrange
      const input = -Infinity;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle very small decimal', () => {
      // Arrange
      const input = 0.0000001;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(true);
      expect(result.value).toBeCloseTo(0.0000002, 10);
    });

    it('should handle maximum safe integer', () => {
      // Arrange
      const input = Number.MAX_SAFE_INTEGER;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(true);
      expect(result.value).toBe(Number.MAX_SAFE_INTEGER * 2);
    });

    it('should handle minimum safe integer', () => {
      // Arrange
      const input = Number.MIN_SAFE_INTEGER;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(true);
      expect(result.value).toBe(Number.MIN_SAFE_INTEGER * 2);
    });

    it('should handle negative zero', () => {
      // Arrange
      const input = -0;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(true);
      expect(result.value).toBe(0);
    });

    it('should handle boolean true as input', () => {
      // Arrange
      const input = true as any;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle boolean false as input', () => {
      // Arrange
      const input = false as any;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle empty string as input', () => {
      // Arrange
      const input = '' as any;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });

    it('should handle numeric string as input', () => {
      // Arrange
      const input = '123' as any;
      
      // Act
      const result = exampleFeature(input);
      
      // Assert
      expect(result.isValid).toBe(false);
      expect(result.error).toBeDefined();
      expect(result.error).toContain('Invalid input');
    });
  });
});
```
