# 🔱 Poseidon: 테스트 코드 (Red)

> **세션 ID**: tdd_2025-11-01_001  
> **작성일**: 2025-11-01  
> **작성자**: Poseidon  
> **단계**: 3단계 - 테스트 코드 작성 (TDD Red)

---

## 1. 📋 테스트 코드 개요

### 1.1 작성 목표

Artemis가 설계한 테스트 시나리오를 기반으로 실제 테스트 코드를 작성합니다.

### 1.2 테스트 파일

- **파일명**: `src/__tests__/unit/easy.repeatIcon.spec.ts`
- **테스트 개수**: 4개
- **예상 결과**: 모두 실패 (Red 단계)

---

## 2. 🧪 작성된 테스트 코드

### 2.1 파일 위치

```
src/__tests__/unit/easy.repeatIcon.spec.ts
```

### 2.2 테스트 구조

```typescript
describe('반복 일정 아이콘 표시', () => {
  describe('주별 뷰', () => {
    it('주별 뷰에서 반복 일정에 Repeat 아이콘이 표시된다');
  });

  describe('월별 뷰', () => {
    it('월별 뷰에서 반복 일정에 Repeat 아이콘이 표시된다');
  });

  describe('일반 일정', () => {
    it('일반 일정에는 Repeat 아이콘이 표시되지 않는다');
  });

  describe('복합 케이스', () => {
    it('알림 + 반복 일정은 두 아이콘이 모두 표시된다');
  });
});
```

---

## 3. 📊 테스트 실행 결과

### 3.1 예상 결과 (Red)

```
✗ 반복 일정 아이콘 표시
  ✗ 주별 뷰에서 반복 일정에 Repeat 아이콘이 표시된다
  ✗ 월별 뷰에서 반복 일정에 Repeat 아이콘이 표시된다
  ✗ 일반 일정에는 Repeat 아이콘이 표시되지 않는다
  ✗ 알림 + 반복 일정은 두 아이콘이 모두 표시된다

Test Files  1 failed (1)
     Tests  4 failed (4)
```

### 3.2 실패 이유

- Repeat 아이콘이 아직 구현되지 않음
- `RepeatIcon` testid가 존재하지 않음
- Hermes가 구현 후 통과 예상

---

## 4. ✅ 체크리스트

- [x] test_spec.md 기반 테스트 코드 작성
- [x] 4개 테스트 케이스 구현
- [x] Vitest + RTL 사용
- [x] 명확한 Given-When-Then 구조
- [x] 적절한 testid 사용

---

**테스트 코드 작성 완료 시각**: 2025-11-01 05:39:00  
**다음 단계**: Hermes (기능 구현 - Green)
