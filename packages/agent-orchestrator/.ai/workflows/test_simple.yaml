name: test_simple
description: Simple 2-step workflow to test persona switching mechanism

# No interactive context needed for quick tests
context:
  interactive: false

steps:
  - persona: analyst
    id: analyze
    args: ['{{featureId}}']

  - persona: analyst
    id: define-success
    args: ['{{featureId}}']
